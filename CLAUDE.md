# py-scratch

## Development rules

- Always bump the version in `.claude-plugin/plugin.json` when making a series of changes in a session. Claude Code only picks up plugin updates when the version changes.
